import React from 'react'
import { StyledFooterWrapper } from '../containers/styled-footer-wrapper'

export const GlobalFooter = () => {
  return <StyledFooterWrapper>Global page footer</StyledFooterWrapper>
}
